spring:
  application:
    name: codex-poetica-backend
  datasource:
    url: jdbc:mysql://localhost:3306/codexpoetica?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: "000000"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
server:
  port: 8080
